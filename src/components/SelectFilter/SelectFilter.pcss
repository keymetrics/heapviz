.SelectFilter {
    margin-bottom: 10px;
    margin-top: 8px;
    & select {
        display: block;
        height: 2rem;
        border: 0px;
        background: var(--contrast);

        &:focus {
            outline: 0px solid var(--primary);
        }
    }
}