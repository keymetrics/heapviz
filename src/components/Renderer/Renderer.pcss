canvas {
    transform: scale(0.5);
    transform-origin: top left;
}

.topCanvas {
    position: absolute;
    top: 0;
    left: 0;
}

.Renderer {
    height: 100vh;
    position: relative;

    & .container {
        overflow: hidden;
        position: relative;
        height: 100%;
    }
}